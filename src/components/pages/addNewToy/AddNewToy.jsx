import useTitle from "../../../hooks/useTitle";

const AddNewToy = () => {
    useTitle('Add New Toy')
    return (
        <div>
            new toy is commig
        </div>
    );
};

export default AddNewToy;