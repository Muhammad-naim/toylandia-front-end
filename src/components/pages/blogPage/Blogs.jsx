import useTitle from "../../../hooks/useTitle";

const Blogs = () => {
    useTitle('Blog')
    return (
        <div>
            <h1>this is blogs</h1>            
        </div>
    );
};

export default Blogs;